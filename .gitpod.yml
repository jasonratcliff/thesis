image:
  file: .github/gitpod.Dockerfile

tasks:
  - name: Install Development Package
    before: Rscript -e "renv::hydrate()"
    init: Rscript -e "renv::install('.')"

  - name: Open R Console
    command: R

vscode:
  extensions:
    - REditorSupport.R
